"""options_backtester — the open-source options backtesting framework.

Imports are populated as modules are implemented. During development,
import directly from subpackages if the top-level re-export is not yet
available.
"""
